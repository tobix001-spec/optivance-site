export type Testimonial = {
  quote: string
  name: string
  role: string
  company: string
  metric: string
}

// No testimonials — only real client references used
export const TESTIMONIALS: Testimonial[] = []
